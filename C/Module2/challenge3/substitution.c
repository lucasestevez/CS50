#include <stdio.h>
#include <strings.h>
#include <stdlib.h>
#include <ctype.h>

int main (int argc, char *args[]) {

        char *key[] = args[1];
        int 

        if (strlen(args) != 26) {
            
            printf("A chave deve conter 26 caracteres");
            return 1;

        } else {
            
            for (int i = 0; i < strlen(args); i++) {
                
            }
            

        }
         

}